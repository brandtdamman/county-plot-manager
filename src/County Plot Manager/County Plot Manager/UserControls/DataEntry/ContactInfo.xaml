<UserControl x:Class="PlotManager.UI.UserControls.DataEntry.ContactInfo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PlotManager.UI.UserControls.DataEntry"
             mc:Ignorable="d" Background="White">

	<UserControl.Resources>
	</UserControl.Resources>

	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto"/>
			<ColumnDefinition Width="Auto"/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<local:Name Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2"
								Margin="10,10,10,5"/>

		<local:EnterAddress Grid.Column="0" Grid.Row="1" Grid.RowSpan="2"
												Margin="10,5,5,10"/>

		<local:EnterPhone Grid.Column="1" Grid.Row="1"
											Margin="5,5,10,5"/>

		<local:EnterEmail Grid.Column="1" Grid.Row="2"
											Margin="5,5,10,10"/>

	</Grid>
</UserControl>
